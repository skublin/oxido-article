Generate structured HTML content from this text in Polish, using onlyHTML tags, e.g. <h1>, <h2>, <p>, <img>, and <em>. Include the same given text with correct Polish characters, section headings, and image placeholders with accurate alt descriptions for AI image generation tools. Avoid adding <html>, <head>, or <body> tags.
